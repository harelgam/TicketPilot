# Secrets — never commit a populated .env
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Ephemeral evaluation output. Curated evidence that the evaluation report cites
# is committed under artifacts/ instead (set TICKETPILOT_RUNS_DIR=artifacts to
# write there), so nothing the report references can go missing.
runs/

# The assignment brief itself — reference material, not project source.
# Remove this line if you would rather ship it alongside the submission.
AI_Take_Home_Assignment_EN.pdf

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
